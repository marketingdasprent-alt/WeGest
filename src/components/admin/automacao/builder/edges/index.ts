import type { EdgeTypes } from '@realflow/react';
import { LigacaoComMais } from './LigacaoComMais';

/** Fora do componente — igual aos nodeTypes, por identidade estável. */
export const edgeTypes: EdgeTypes = { comMais: LigacaoComMais };
